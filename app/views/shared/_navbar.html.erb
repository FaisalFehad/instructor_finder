<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Instructor Finder</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <form class="navbar-form navbar-left">
        <div class="input-group">
          <%= form_tag(root_path, :method => "get", id: "search-form") do %>
            <%= text_field_tag :search, params[:search], placeholder: "Look for a specific location", class: 'form-control' %>
            <span class="input-group-btn">
              <%= submit_tag "Search", class: 'btn btn-default' %>
            </span>
          <% end %>
        </div>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% if admin_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin Control <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "New instructor", new_instructor_path %></li>
              <li><%= link_to "Requests history", history_url %></li>
              <li><%= link_to "Listed instructors", instructors_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Edit your account", edit_admin_registration_path %></li>
              <li><%= link_to "Sign out", destroy_admin_session_path, method: :delete %></li>
            </ul>
          <% else %>
            <li><%= link_to "Admin Log in", new_admin_session_path %></li>
          <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
